#!/bin/bash
#Generated by JetBrains Toolbox 3.5.0.84344 at 2026-06-05T23:01:09

declare -a intellij_args=()
declare -- wait=""

for o in "$@"; do
  if [[ "$o" = "--wait" || "$o" = "-w" ]]; then
    wait="-W"
    o="--wait"
  fi
  if [[ "$o" =~ " " ]]; then
    intellij_args+=("\"$o\"")
  else
    intellij_args+=("$o")
  fi
done

open -na "/Users/jakub.konas/Applications/Fleet.app/Contents/MacOS/Fleet" $wait --args "${intellij_args[@]}"